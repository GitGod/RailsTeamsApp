<div class="row">
<% @teams.each do |team| %>
  <div class="col-md-6">
    <div class="card flex-md-row mb-4 box-shadow">
      <% if team.logo.exists? %>
        <%= image_tag(team.logo.url(:thumb), class: "card-img-left flex-auto d-none d-lg-block") %>
      <% else %>
        <%= holder_tag '150x150', team.name, 'sky', { class: 'card-img-right flex-auto d-none d-lg-block' }, { auto: 'yes' } %>
      <% end %>

      <div class="card-body d-flex flex-column align-items-start">
        <h4><%= link_to team.name, team, class: "text-dark" %></h4>
        <p class="card-text text-muted"><%= team.city %></p>
        <div class="d-flex justify-content-between align-items-center">
          <div>
            <%= link_to 'Edit', edit_team_path(team), class: "btn btn-sm btn-outline-primary" %>
            <%= link_to 'Delete', team, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-sm btn-outline-danger" %>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>
</div>
