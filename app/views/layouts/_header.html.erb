<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container">
    <%= link_to "RailsTeamsApp", root_path, { :class=>"navbar-brand" } %>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <div class="navbar-nav">
        <li class="nav-item <%= 'active' if params[:controller] == 'players' %>">
          <%= link_to 'Players', players_path, { :class=>"nav-link" } %>
        </li>
        <li class="nav-item <%= 'active' if params[:controller] == 'teams' %>">
          <%= link_to 'Teams', teams_path, { :class=>"nav-link" } %>
        </li>
        <li class="nav-item <%= 'active' if params[:controller] == 'leagues' %>">
          <%= link_to 'Leagues', leagues_path, { :class=>"nav-link" } %>
        </li>
      </div>
    </div>

  </div>
</nav>
